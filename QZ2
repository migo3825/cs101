#include <stdio.h>

void print_spaces(int r, int rows) {
		printf(" ");
		printf("\n");
    
    int i;
}

void print_stars(int r) {
		 printf("*"); 
    int i;
}

int main() {
    int rows = 5,r;
    for(int i=rows;i>=1;--i){
        print_space(i);
        print_stars(i);
    }
    return 0;
}
